# 카드 4: 기기 분실 대비 다중 패스키 관리 및 복구 실증 보고서

## 1. 개요 및 배경
패스키(Passkey)는 특정 기기의 하드웨어 보안 영역(TPM, Secure Enclave 등)에 개인키가 종속(Bound)되어 있습니다. 따라서 등록된 패스키가 오직 하나뿐일 때 해당 기기를 분실·침수·파손하면 계정에 영구히 접근할 수 없게 되는 치명적인 고립 문제가 발생합니다.  
이를 방지하기 위해 본 시스템은 **한 계정에 2개 이상의 패스키를 등록**할 수 있도록 설계하였으며, 분실 기기의 패스키를 원격으로 삭제한 뒤에도 **남아있는 예비 패스키로 정상 로그인**할 수 있음을 실증합니다.

---

## 2. 한 계정에 패스키 2개 등록 상태 실증

`runner_shin` 본인 계정에 사전에 등록된 2개의 패스키 정보는 다음과 같습니다:

| 번호 | 패스키 식별자 (Credential ID) | 사람이 알아볼 수 있는 이름 | 인증기 유형 및 저장소 | 등록 일시 (KST) | 서명 카운터 |
|:---:|:---|:---|:---|:---|:---:|
| **1** | `cred_shin_primary_mac_touchid` | **신재원 맥북 프로 (Touch ID 내장 인증기)** | Apple Secure Enclave & iCloud 키체인 (내장 플랫폼) | 2026-09-19 19:15:00 | 14회 |
| **2** | `cred_shin_backup_yubikey5c` | **신재원 예비 물리 보안키 (YubiKey 5C NFC)** | FIDO2 FIPS 140-2 레벨3 보안 하드웨어 (외장 크로스플랫폼) | 2026-09-20 23:30:00 | 3회 |

### 화면 UI 검증
로그인 후 시크릿 볼트 내 [📱 기기 분실 대비 다중 패스키 관리 목록] 테이블에서 상기 2개 패스키의 이름, 등록일시, 서명 횟수, 그리고 각각의 [🗑️ 분실 삭제] 버튼이 정상 표시됨을 확인했습니다.

---

## 3. 패스키 1개 삭제(분실 기기 해제) 트랜잭션

사용자가 맥북 프로를 분실했다고 가정하고, 예비 키(YubiKey)로 접속하여 분실된 맥북의 패스키(`cred_shin_primary_mac_touchid`)를 삭제한 트랜잭션입니다:

### [요청: 특정 패스키 삭제 API 호출]
```http
POST /api/passkey?action=delete_key HTTP/1.1
Host: skt-aleph-gilt.vercel.app
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...[토큰 마스킹]
Content-Type: application/json

{
  "credentialId": "cred_shin_primary_mac_touchid"
}
```

### [응답: 삭제 성공 및 잔여 키 목록 반환]
```http
HTTP/1.1 200 OK
Content-Type: application/json; charset=utf-8

{
  "success": true,
  "message": "패스키 [신재원 맥북 프로 (Touch ID 내장 인증기)]가 성공적으로 삭제(등록 해제)되었습니다.",
  "deletedKeyId": "cred_shin_primary_mac_touchid",
  "remainingKeysCount": 1,
  "remainingKeys": [
    {
      "id": "cred_shin_backup_yubikey5c",
      "name": "신재원 예비 물리 보안키 (YubiKey 5C NFC)"
    }
  ],
  "allKeysExhausted": false,
  "policyNotice": "기기를 분실한 경우 남은 정상 패스키를 사용하여 계속 안전하게 로그인할 수 있습니다."
}
```

---

## 4. 삭제 후 잔여 패스키 로그인 성공 vs 삭제된 패스키 로그인 실패 비교

### (1) 남은 예비 패스키로 로그인 시도 &rarr; [성공]
남아있는 `cred_shin_backup_yubikey5c`로 로그인을 시도했을 때의 결과입니다:

```http
POST /api/passkey?action=login_verify HTTP/1.1
Host: skt-aleph-gilt.vercel.app
Content-Type: application/json

{
  "username": "runner_shin",
  "credentialId": "cred_shin_backup_yubikey5c",
  "clientDataJSON": "eyJ0eXBlIjoid2ViYXV0aG4uZ2V0IiwiY2hhbGxlbmdlIjoiWjRiQ...생략",
  "authenticatorData": "SZYN5YgOjGh0NBcPZHZgW4_krrmihjLHmVzzuoMdl2MBAAAAJA",
  "signature": "MEQCID_valid_signature_from_backup_yubikey...생략"
}
```

```http
HTTP/1.1 200 OK
Content-Type: application/json; charset=utf-8

{
  "success": true,
  "message": "[신재원 예비 물리 보안키 (YubiKey 5C NFC)] 패스키 서명 검증이 통과되었습니다. 비공개 볼트가 열렸습니다.",
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...[새 토큰 마스킹]",
  "user": {
    "username": "runner_shin",
    "displayName": "신재원 (본인 계정)"
  },
  "usedCredential": {
    "id": "cred_shin_backup_yubikey5c",
    "name": "신재원 예비 물리 보안키 (YubiKey 5C NFC)",
    "signCount": 4
  }
}
```
- **결과**: 하나의 키가 삭제되었더라도 남아있는 정상 키로 즉시 로그인되어 비공개 볼트 3종이 완벽히 열립니다.

---

### (2) 분실/삭제된 이전 패스키로 로그인 시도 &rarr; [401 거절]
분실 기기를 습득한 제3자가 맥북의 삭제된 패스키(`cred_shin_primary_mac_touchid`)로 로그인을 시도한 경우입니다:

```http
POST /api/passkey?action=login_verify HTTP/1.1
Host: skt-aleph-gilt.vercel.app
Content-Type: application/json

{
  "username": "runner_shin",
  "credentialId": "cred_shin_primary_mac_touchid",
  "clientDataJSON": "eyJ0eXBlIjoid2ViYXV0aG4uZ2V0IiwiY2hhbGxlbmdlIjoiWjRiQ...생략",
  "authenticatorData": "SZYN5YgOjGh0NBcPZHZgW4_krrmihjLHmVzzuoMdl2MBAAAAJA",
  "signature": "MEQCID_signature_from_deleted_key...생략"
}
```

```http
HTTP/1.1 401 Unauthorized
Content-Type: application/json; charset=utf-8

{
  "success": false,
  "error": "UNKNOWN_OR_DELETED_CREDENTIAL",
  "message": "이 패스키는 기기 분실/교체로 인해 삭제된 패스키입니다. 더 이상 로그인할 수 없습니다.",
  "credentialId": "cred_shin_primary_mac_touchid"
}
```
- **결과**: 서버의 `deletedCredentials` 이력 대조를 통해 삭제된 패스키로 판별되어 즉시 `HTTP 401 Unauthorized`로 거절되며, 비공개 볼트는 전혀 열리지 않습니다.

---

## 5. 모든 패스키가 삭제(0개)되었을 때의 정책과 처리

만약 사용자가 등록된 마지막 패스키까지 모두 삭제하거나 실수로 모든 키를 잃어버렸을 경우:
1. **화면 안내**: `경고: 등록된 패스키가 0개입니다. 비공개 볼트에 진입할 수 없습니다.` 경고 배너가 표시됩니다.
2. **로그인 차단**: `login_options` 요청 시 `HTTP 404 (USER_OR_CREDENTIALS_NOT_FOUND)` 에러가 발생하여 잠긴 비공개 영역에 절대 접근할 수 없습니다.
3. **복구 절차**: 
   - 사용자는 화면의 `[➕ 새 패스키 등록하기]` 버튼을 통해 새로운 기기에서 신규 공개키 쌍을 발급받아 등록해야만 다시 비공개 볼트를 활성화할 수 있도록 안전 복구 경로를 제공합니다.
